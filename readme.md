# Pre
[Bun](https://bun.sh/)

## To Run

1. create `.env` file with neon db connection string
2. run `bun index.ts`

## To Seed

1. create `.env` file with neon db connection string
2. run `bun db/seed.ts`
